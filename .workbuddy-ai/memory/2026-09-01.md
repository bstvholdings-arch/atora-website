# 2026-09-01

## ATORA GEO + AI Search optimization (Phase 4) — completed & verified
- Built a fabrication-free GEO layer so AI engines (ChatGPT/AOAI/Gemini/Perplexity/Claude/Copilot) cite ATORA accurately.
- New files: `src/lib/seo.ts` (buildPageMetadata, SITE_URL, HREFLANG), `src/lib/schema.ts` (all JSON-LD builders), `src/components/JsonLd.tsx` (escapes `<>&`), `src/components/HtmlLang.tsx` (sets `<html lang>` client-side), `src/app/llms.txt/route.ts`, `src/app/[lang]/aircond-wholesale-malaysia/page.tsx` (GEO content hub).
- Injected JSON-LD + buildPageMetadata on every `[lang]` page (home, about, brands list+detail, products list+detail, parts, contact, faq, locations, project-supply, technical-partners list+detail). Layout emits Organization + WebSite + LocalBusiness (per DB location).
- Rewrote `robots.txt` (allows GPTBot/OAI-SearchBot/PerplexityBot/ClaudeBot/Google-Extended/Applebot-Extended/Amazonbot/cohere/Diffbot; blocks /admin, /api, *.env, query-param URLs). Rewrote `sitemap.xml` with valid BCP-47 hreflang (en-MY/ms-MY/zh-MY + x-default).
- Fixed real data bug: seeded categories had `parent_id=NULL` despite hierarchical slugs → added `effectiveParentId`/`childCategoryIds`/`listChildCategories` + JS-side group expansion in `searchProducts` (homepage/products now render correctly).
- Added `resolveBrand` (brand_id + case-insensitive name-prefix fallback for 14 legacy NULL-brand_id products).
- **No-fake-data correction:** `seo_default_description_en` (DB), `layout.tsx`/`settings.ts` defaults, and en/bm/zh `why1Desc`+`a2` message strings listed "Mitsubishi Electric, Topaire" as supplied brands — but the brands table has only 8 (Midea, Haier, Hisense, Daikin, Acson, Panasonic, AUX, TCL). Updated blurb to list the 8 real brands (added missing TCL). NOTE: "Mitsubishi Electric" and "Topaire" exist ONLY as legacy product SKUs (products table, NULL brand_id) + a DB FAQ answer — they are real products, NOT catalogued brands, so no brand pages exist for them. GEO rule: ATORA is independent multi-brand supplier; NEVER claim "official distributor"/"authorised dealer".
- Build passes (`npm run build`, typecheck + lint clean). Verified live on port 3000: robots/sitemap/llms.txt, JSON-LD types per page, hreflang in <head>, content-language en-MY/ms-MY/zh-MY, no "official distributor" claims.

## Carry-over (user action required)
- Sandbox `git push` blocked by Git Credential Manager browser auth. User must commit + `git push origin main` locally, add Vercel env vars (DATABASE_URL, NEXT_PUBLIC_SITE_URL, SUPABASE_URL/ANON/SERVICE_ROLE if uploads enabled), then deploy. Changes are currently UNCOMMITTED.
